# Sudachi分析結果の精度評価レポート

## 概要
独自に設計したSudachiを使用した形態素分析結果（CSV）と、既存のデータベース（DB）に格納されているコーディング結果を比較・検証しました。

## 分析結果サマリー
- **総段落数**: 22,183
- **完全一致数**: 13,893
- **精度 (Accuracy)**: **62.63%**

## 主な相違点と原因分析

### 1. 検出漏れ (False Negatives)
DBには存在するが、Sudachi分析結果（CSV）には含まれていないコードが多数確認されました。特に以下のコードが顕著です。

| コード | 欠損数 | 備考 |
| :--- | :--- | :--- |
| `*MODAL_MUST` | 3,613 | 義務表現（「しなければならない」等）が完全に検出されていません。 |
| `*CLAUSE_EXPLANATION` | 2,040 | 定義や目的などの説明条項が検出されていません。 |
| `*CLAUSE_DOC_REQUIREMENTS` | 796 | 書類要件に関する条項の検出漏れ。 |
| `*CLAUSE_POSITIVE_PERMISSION_CONSENT` | 718 | 許可・同意に関する条項の検出漏れ。 |
| `*MODAL_PROHIBIT` | 476 | 禁止表現の検出漏れ。 |

**原因**:
- **単一ラベル vs マルチラベル**: DBでは1つの段落に複数のコード（例: テーマ + モダリティ）が付与されていますが、Sudachi分析結果（CSV）は**1段落につき1つのコード**しか出力していません。
- **特定のコードの除外**: `*MODAL_MUST` や `*CLAUSE_EXPLANATION` はCSV内に一度も出現しません。分析ロジックまたは出力フィルタリングにおいて、これらのコードが意図的に除外されているか、他の「テーマ」コード（例: `*CLAUSE_DECOMMISSION_MAINTENANCE`）が優先されて上書きされている可能性が高いです。一方で、`*MODAL_SHOULD`（努力義務）は検出されています。

### 2. 過剰検出 (False Positives)
DBには存在しないが、Sudachi分析結果には含まれているコードも一部確認されましたが、検出漏れに比べると少数です。

| コード | 過剰数 | 備考 |
| :--- | :--- | :--- |
| `*CLAUSE_DRAWINGS` | 209 | 図面に関する条項。 |
| `*PROCEDURE_NOTICE_MUNICIPARITY` | 133 | 市町村への通知手続き。 |

## 結論
現在のSudachi分析は、段落の主要な「テーマ」を特定することには一定の成功（約6割の一致）を収めていますが、**「義務（MUST）」や「禁止（PROHIBIT）」といったモダリティ（法的拘束力）の判定機能が欠落している**、または出力に含まれていません。
高精度な分析を実現するためには、**マルチラベル出力への対応**と、**モダリティ判定ロジックの見直し**が必要です。
